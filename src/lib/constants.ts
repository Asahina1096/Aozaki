/**
 * Shared style constants for consistent UI across components
 */

export const PILL_STYLES = {
  info: "inline-flex items-center gap-0.5 rounded-full border border-border/15 bg-muted/70 px-1 py-0.5 whitespace-nowrap text-[0.65rem]",
  status:
    "inline-flex items-center gap-0.5 rounded-full border border-border/15 bg-muted/70 px-1 py-0.5 whitespace-nowrap text-[0.65rem]",
  network:
    "inline-flex items-center gap-1 rounded-full border border-border/15 bg-muted/70 px-1.5 py-0.5 whitespace-nowrap",
} as const;
